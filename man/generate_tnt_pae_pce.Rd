% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_tnt_pae_pce.R
\name{generate_tnt_pae_pce}
\alias{generate_tnt_pae_pce}
\title{generate_tnt_pae_pce}
\usage{
generate_tnt_pae_pce(
  matrix_file = "out_TNT/pres_abs.tnt",
  output_file = "PAE_PCE_tntAUTO.run",
  max_iterations = 10,
  search_replicates = 4,
  search_method = "new_technology",
  output_dir = "out_TNT/"
)
}
\arguments{
\item{matrix_file}{Character. Path to the TNT matrix file (default: "out_TNT/pres_abs.tnt")}

\item{output_file}{Character. Name of the output TNT script file (default: "PAE_PCE_tntAUTO.run")}

\item{max_iterations}{Integer. Maximum number of PAE-PCE rounds to perform (default: 10)}

\item{search_replicates}{Integer. Number of search replicates to perform (default: 4)}

\item{search_method}{Character. Search method to use: "traditional" or "new_technology" (default: "new_technology")}

\item{output_dir}{Character. Directory for output files (default: "out_TNT/")}
}
\value{
Invisibly returns the path to the generated script file
}
\description{
Creates a TNT script file for automated PAE-PCE (Parsimony Analysis of Endemicity
with Parsimony Constraint) analysis with multiple rounds of character removal.
}
\details{
This function generates a TNT script that performs iterative PAE-PCE analysis:

\strong{Search Methods:}
\itemize{
\item \code{"traditional"}: Uses traditional search with TBR branch swapping
\itemize{
\item Command: \verb{mult 1000 = hold 100 tbr; bb=tbr;}
}
\item \code{"new_technology"}: Uses new technology search with sectorial searches
\itemize{
\item Command: \verb{xmult= hits 7 rep 5 rss level 10;}
}
}

\strong{Iterative Process:}
\enumerate{
\item Performs parsimony analysis
\item Calculates CI (Consistency Index) and RI (Retention Index) for all characters
\item Identifies non-homoplastic synapomorphies (CI=1, RI=1)
\item Deactivates these characters cumulatively
\item Repeats until no more synapomorphies are found or max_iterations is reached
}

\strong{Output Files (per round N):}
\itemize{
\item \code{Round_OutputN.txt}: Complete log of the round
\item \code{Round_CI_RI_TableN.txt}: CI/RI table for all characters
\item \code{Round_SynapomorphiesN.txt}: List of non-homoplastic synapomorphies
\item \code{Round_MPTN.tre}: Most parsimonious trees
\item \code{Round_ConsensusN.tmp}: Consensus tree (TNT format)
\item \code{Round_TreeN.tre}: Consensus tree (Newick format)
\item \code{Round_MapN.svg}: Tree visualization with mapped synapomorphies
}

\strong{Summary Files:}
\itemize{
\item \code{PAE_PCE_Summary.txt}: Summary of all rounds
\item \code{resul_CI_RI.log}: CI/RI statistics for all rounds
}
}
\examples{
\dontrun{
# Generate script with default settings (4 replicates, new technology)
generate_tnt_pae_pce()

# Generate script with 20 replicates and traditional search
generate_tnt_pae_pce(
  search_replicates = 20,
  search_method = "traditional"
)

# Generate script with custom settings
generate_tnt_pae_pce(
  matrix_file = "data/my_matrix.tnt",
  output_file = "my_pae_pce.run",
  max_iterations = 15,
  search_replicates = 10,
  search_method = "new_technology"
)
}

}
